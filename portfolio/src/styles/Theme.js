import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#241d20;',
    },
    secondary: {
      main: '#535252',
    },
  },
});

export default theme;
